#include<iostream>

#include<math> //this to use the math library


int main(){
	/*
	here you have some useful math functions
	max(x, y)
	min(x, y)
	sqrt(x)
	round(x)
	log(x)
	pow(x, y)
	ceil(x)
	flor(x)
	*/
	return 0;
}
